@model List<Avaliacao.Models.LojasViewModel>

@{
    ViewData["Title"] = "Index";
}
    
<h1>Index</h1>
    
@if(!(Model.Count == 0))
{
    <hr>
    @foreach(var loja in Model)
    {
    <h1>@loja.Nome</h1>
    <hr>
    }
}
else
{
    <p>Nenhum registro!</p>
}